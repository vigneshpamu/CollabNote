/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ['ythccrelmioiwmjsxyje.supabase.co'],
  },
}

export default nextConfig
